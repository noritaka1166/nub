{
  "name": "conform-overrides-nested",
  "version": "1.0.0",
  "private": true,
  "description": "Drop-in PM conformance fixture: a scoped (nested) pnpm override. 'debug>ms' pins ms to 2.1.3 only under debug, declared in pnpm-workspace.yaml; pnpm records it in the lockfile's overrides: block, which nub must round-trip in both directions.",
  "dependencies": {
    "debug": "^4.4.0"
  }
}
