{
  "name": "conform-optional-deps",
  "version": "1.0.0",
  "private": true,
  "description": "Drop-in PM conformance fixture: optionalDependencies with platform-conditional installs. esbuild bundles per-platform optionals in its own package; this exercises optional-dep resolution and the trust-floor postinstall path (esbuild runs a postinstall to select its platform binary).",
  "dependencies": {
    "ms": "^2.1.3"
  },
  "optionalDependencies": {
    "fsevents": "^2.3.3"
  }
}
