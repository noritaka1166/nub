{
  "name": "conform-simple",
  "version": "1.0.0",
  "private": true,
  "description": "Drop-in PM conformance fixture: plain registry deps with a transitive overlap. debug→ms and ms are both direct, exercising version-range deduplication.",
  "dependencies": {
    "debug": "^4.4.0",
    "kleur": "^4.1.5",
    "ms": "^2.1.3"
  },
  "devDependencies": {
    "type-fest": "^4.0.0"
  }
}
