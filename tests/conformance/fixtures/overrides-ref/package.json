{
  "name": "conform-overrides-ref",
  "version": "1.0.0",
  "private": true,
  "description": "Drop-in PM conformance fixture: a pnpm.overrides entry using a $-ref ($ms = 'use the version of ms from the root deps'). pnpm records the override in the lockfile as the resolved value (^2.1.3) while package.json keeps the literal $ms; the frozen drift check must resolve the $-ref before comparing (issue #16).",
  "dependencies": {
    "ms": "^2.1.3",
    "debug": "^4.4.0"
  },
  "pnpm": {
    "overrides": {
      "ms": "$ms"
    }
  }
}
