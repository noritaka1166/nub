---
title: npm
description: Nub speaks npm's on-disk formats — the version-2 and version-3 lockfile round-trips byte-for-byte, npm workspaces and overrides are honored, and npm config is read across the builtin, global, user, and project scopes. The CLI is pnpm-shaped; the files are npm's.
---

The CLI you type is pnpm-shaped; the files Nub reads and writes are npm's. When npm is the incumbent — a `packageManager: "npm@…"` field or an existing `package-lock.json` — Nub reads and writes npm's own `package-lock.json` in its native format, honors npm's config cascade, and introduces no `pnpm-lock.yaml`. Everything below is gated on that.

<CompatTable
  incumbent="npm"
  rows={[
    // ── Commands ──────────────────────────────────────────────────────
    { feature: <><code>install</code> / <code>i</code> / <code>ci</code></>, status: 'yes', note: 'Resolves, links, and writes package-lock.json on the real engine. ci installs strictly from the lockfile.' },
    { feature: <><code>install &lt;pkg&gt;</code> / <code>uninstall</code> / <code>update</code></>, status: 'yes', note: 'Mutate the dependency graph, rewrite package.json, and update the lockfile. Aliases add/a, remove/rm/un, up all resolve.' },
    { feature: <><code>import</code></>, status: 'yes', note: 'Converts an existing pnpm, Yarn, or Bun lockfile into a package-lock.json.' },
    { feature: <><code>dedupe</code> / <code>prune</code></>, status: 'yes', note: 'dedupe collapses duplicate ranges; prune removes extraneous packages.' },
    { feature: <><code>rebuild</code> / <code>fetch</code></>, status: 'yes', note: 'rebuild reruns build scripts for installed packages; fetch populates the store from the lockfile without linking.' },
    { feature: <><code>link</code> / <code>unlink</code></>, status: 'yes', note: 'Symlink a local package into a project and remove it again.' },
    { feature: <><code>ls</code> / <code>why</code> / <code>outdated</code></>, status: 'yes', note: 'Read-only queries over the installed tree: ls (list/la/ll), dependency paths (why), and out-of-date packages.' },
    { feature: <><code>view</code> / <code>search</code></>, status: 'yes', note: 'view (info/show) reads registry metadata; search delegates to npm.' },
    { feature: <><code>audit</code> / <code>licenses</code></>, status: 'yes', note: 'Vulnerability audit and license report run on the real engine.' },
    { feature: <><code>publish</code> / <code>pack</code> / <code>version</code></>, status: 'yes', note: 'Registry publish, tarball packing, and version bumping.' },
    { feature: <><code>dist-tag</code> / <code>deprecate</code> / <code>unpublish</code></>, status: 'yes', note: 'Registry tag management, deprecation, and unpublish.' },
    { feature: <><code>login</code> / <code>logout</code></>, status: 'yes', note: 'Registry auth, writing the token into the npmrc cascade.' },
    { feature: <><code>config</code> / <code>get</code> / <code>set</code></>, status: 'yes', note: 'Read and write config across the npmrc scopes.' },
    { feature: <><code>cache</code></>, status: 'yes', note: 'Content-addressed store and cache forensics.' },
    { feature: <><code>exec</code> / <code>dlx</code></>, status: 'yes', note: 'Fetch-and-run a package in a temp environment; nubx is the run-it-once spelling.' },
    { feature: <><code>create</code></>, status: 'yes', note: 'Scaffold from a create-* starter.' },
    { feature: <><code>run</code> + lifecycle</>, status: 'yes', note: 'package.json scripts run through nub run with pre/post lifecycle ordering.' },
    // ── Dependency fields ─────────────────────────────────────────────
    { feature: <><code>dependencies</code> / <code>devDependencies</code></>, status: 'yes', note: 'Resolved and installed exactly as npm does, with the dev/prod split honored by --omit=dev / --production.' },
    { feature: <><code>optionalDependencies</code></>, status: 'yes', note: 'Installed when the platform matches; skipped silently otherwise, like npm.' },
    { feature: <><code>peerDependencies</code> / <code>peerDependenciesMeta</code></>, status: 'yes', note: 'Peer ranges resolved per npm rules; peerDependenciesMeta.optional suppresses missing-peer warnings.' },
    { feature: <code>overrides</code>, status: 'yes', note: 'npm\'s own override field, applied during resolution and written into the lockfile (npm 8.3+).' },
    { feature: <code>resolutions</code>, status: 'yes', note: 'npm ignores this Yarn-style field; Nub mirrors that and drops it with a warning. Use overrides.' },
    { feature: <><code>engines</code> / <code>os</code> / <code>cpu</code> / <code>libc</code></>, status: 'yes', note: 'Platform fields enforced during resolution; engine-strict honored.' },
    { feature: <><code>packageManager</code></>, status: 'yes', note: 'A packageManager: npm@… declaration selects the npm incumbent.' },
    { feature: <><code>bundleDependencies</code></>, status: 'yes', note: 'Bundled deps round-trip through the lockfile and tarball, as npm packs them.' },
    // ── Workspaces ────────────────────────────────────────────────────
    { feature: <><code>workspaces</code></>, status: 'yes', note: 'The neutral workspaces array (and the {packages: […]} object form) defines membership, like npm.' },
    { feature: <>workspace ranges</>, status: 'yes', note: 'Local members are linked and a published range is written, matching npm\'s workspace resolution.' },
    { feature: <>workspace selectors</>, status: 'yes', note: 'Name, glob, directory, exclusion, and dependency traversal selectors work, plus -r and --include-workspace-root. npm\'s own --workspace / --workspaces flags resolve too.' },
    // ── Config files ──────────────────────────────────────────────────
    { feature: <>project + user <code>.npmrc</code></>, status: 'yes', note: 'Registry, scoped registries, auth tokens, TLS (cafile, ca, strict-ssl), proxy, and install-shaping keys are all read.' },
    { feature: <>global + builtin <code>.npmrc</code></>, status: 'yes', note: 'The $PREFIX/etc/npmrc and npm-builtin scopes are read, with npm\'s precedence: CLI flags, env, project, user, global, builtin.' },
    // ── Environment variables ─────────────────────────────────────────
    { feature: <><code>npm_config_*</code></>, status: 'yes', note: 'The npm-config env surface (npm_config_registry, npm_config_cache, …) overrides the corresponding .npmrc key.' },
    { feature: <><code>NPM_CONFIG_*</code> / <code>NPM_TOKEN</code></>, status: 'yes', note: 'Upper-case config vars and the registry auth token are read.' },
    // ── Lockfiles ─────────────────────────────────────────────────────
    { feature: <><code>package-lock.json</code> v3</>, status: 'yes', note: 'Read and write in npm\'s native lockfileVersion 3 (npm 9+ default), accepted by npm ci byte-for-byte.' },
    { feature: <><code>package-lock.json</code> v2</>, status: 'yes', note: 'Read and reproduced byte-for-byte on a no-op install; the first mutating install rewrites it as v3.' },
    { feature: <><code>npm-shrinkwrap.json</code></>, status: 'yes', note: 'Read and preserved as-is alongside or in place of package-lock.json.' },
    { feature: <>lockfileVersion 1</>, status: 'no', note: 'npm 5–6 format. Re-lock under npm 7+ first; a v1 lockfile is refused rather than misread.' },
    { feature: <>pnpm / Bun lockfile round-trip</>, status: 'yes', note: 'pnpm-lock.yaml and bun.lock round-trip via import; yarn.lock is read-only (writes refused).' },
    // ── Install behavior ──────────────────────────────────────────────
    { feature: <>hoisted <code>node_modules</code></>, status: 'yes', note: 'The flat npm-style layout is the default under an npm incumbent.' },
    { feature: <>lifecycle build scripts</>, status: 'yes', note: 'Run under a deny-by-default trust posture with a curated default-trust floor, not npm\'s run-everything default; nub approve-builds adds an entry. Override per package as needed.' },
    { feature: <>content-addressed store</>, status: 'yes', note: 'A global content-addressed store with hardlink/clone linking into node_modules, saving disk over npm\'s per-project copies.' },
  ]}
/>

## `package-lock.json`

Nub reads `lockfileVersion` 2 and 3, resolves against it, and writes it back in npm's exact format. On a no-op install the writer reproduces npm's output byte-for-byte — key ordering, the `devOptional` collapse, `peerDependenciesMeta.optional`, and platform fields all preserved — so the file doesn't churn and `npm ci` accepts it unchanged.

```jsonc
// package-lock.json written by `nub install` — accepted by `npm ci` byte-for-byte
{
  "name": "app",
  "version": "1.0.0",
  "lockfileVersion": 3,
  "requires": true,
  "packages": {
    "": { "name": "app", "version": "1.0.0", "dependencies": { "is-odd": "3.0.1" } },
    "node_modules/is-odd": {
      "version": "3.0.1",
      "resolved": "https://registry.npmjs.org/is-odd/-/is-odd-3.0.1.tgz",
      "integrity": "sha512-CQpnWPrDwmP1+SMHXZhtLtJv90yiyVfluGsX5iNCVkrhQtU3TQHsUWPG9wkdk9Lgd5yNpAg9jQEo90CBaXgWMA==",
      "license": "MIT",
      "dependencies": { "is-number": "^6.0.0" },
      "engines": { "node": ">=4" }
    }
  }
}
```

A no-op rewrite keeps the incoming `lockfileVersion` — a v2 lock stays v2. The first **mutating** operation (`nub add`, `nub remove`) rewrites it as v3 (npm's default since npm 9), so the version field is an expected diff. An `npm-shrinkwrap.json` is read and preserved as-is.

Older formats stop at v1. A `lockfileVersion: 1` lock (npm 5–6) is refused before install rather than silently misread:

```console
# captured: nub 0.0.44 on a lockfileVersion 1 package-lock.json
$ nub install
ERR_NUB_LOCKFILE_PARSE

  × failed to parse lockfile          # ❌ v1 is unsupported — install refused
  ╰─▶ failed to parse lockfile /path/to/package-lock.json:
      package-lock.json lockfileVersion 1 is not supported (need v2 or v3)
```

Run `npm install` once under npm 7+ to upgrade the lock to v2/v3, then `nub install`.

## `.npmrc`

When npm is the incumbent, Nub reads npm's `.npmrc` cascade:

```
builtin   npm's builtin npmrc
global    $PREFIX/etc/npmrc, or NPM_CONFIG_GLOBALCONFIG
user      ~/.npmrc, or NPM_CONFIG_USERCONFIG
project   ./.npmrc
```

Everything at those scopes works — default and scoped registries, auth tokens, TLS, proxies, and install-shaping keys like `engine-strict` and `save-exact`. The `npm_config_*` env vars drive install behavior too. Precedence matches npm, highest first: CLI flags, then `npm_config_*`, then project, user, global, and builtin `.npmrc`.

### Custom CAs

Corporate or self-signed certificate authorities are configured through the standard npm TLS keys, top-level or scoped to one registry:

```ini
# .npmrc
cafile=./corp-ca.pem                          # PEM bundle, all registries
ca="-----BEGIN CERTIFICATE-----..."           # inline PEM (repeat ca[]= to stack)
//registry.example.com/:cafile=./corp-ca.pem  # per-registry override
strict-ssl=false                              # disable verification (user/global scope only)
```

The certificates are added to the trust store of the install client. A committed project `.npmrc` cannot turn off `strict-ssl` — only the user or global scope can.

## Config

When npm is the incumbent, Nub honors npm's graph-shaping fields:

- `overrides` — npm's own version-pin field (npm 8.3+), applied during resolution and written into the lockfile.
- `workspaces` — the neutral membership array, plus npm's `--workspace` / `--workspaces` flags.
- `engines` / `os` / `cpu` / `libc` — platform fields enforced during resolution.

```json
// package.json
{
  "overrides": { "is-number": "7.0.0" },
  "workspaces": ["packages/*"]
}
```

npm ignores the Yarn-style top-level `resolutions`, so Nub drops it with a warning under an npm incumbent. Use `overrides`.

## Install behavior

The default `node_modules` layout is **hoisted** — the flat npm-style scheme.

Lifecycle scripts run under Nub's deny-by-default trust posture plus a gated default-trust floor (see [Security](/docs/install#security)), not npm's run-everything default. Curated, age-vetted packages build automatically; everything else is skipped with `WARN_NUB_IGNORED_BUILD_SCRIPTS` until `nub approve-builds`. Platform-gated optionals (e.g. `fsevents`) resolve and round-trip into the lock.

## Gaps

- **lockfileVersion 1** (npm ≤ 6) errors, not migrated — run `npm install` once to upgrade first.
- **A v2 lock becomes v3** on the first mutating install (preserved on no-op installs only).
- **No drop-in `npm` CLI grammar.** npm-specific subcommand spellings and flags are not emulated; the CLI is pnpm-shaped.
